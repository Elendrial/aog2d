package me.elendrial.aog2d.objects.tiles;

import me.hii488.gameObjects.tiles.BaseTile;

// Currently unused other than to remove need for functions, may remove if not needed for anything else. (It probably will, for movement etc, but that'll be a later system)
public abstract class AoGTile extends BaseTile {
	
	public void onLoad() {}
	public void onUnload() {}
	public void updateOnTick() {}
	public void updateOnSec() {}
	
}
